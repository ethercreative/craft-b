<?php

return [
	/**
	 * ### Atoms Root
	 *
	 * _Defaults to `_atoms`_
	 *
	 * The location of your atom templates, relative to your `templates`
	 * directory. Used by the `atom` twig tag.
	 */
	'atoms' => '_atoms',

	/**
	 * ### Critical Root
	 *
	 * _Defaults to `_critical`_
	 *
	 * The location of your critical css files, relative to your `templates`
	 * directory. Used by the `critical` twig tag.
	 */
	'critical' => '_critical',
];
